<div class="wrapper">

<div>
  <h1 class="section__title--collection"><%= @collection.title %></h1>
</div>

  <div class="route-card__container">
    <% @routes.each do |route| %>
      <%= render 'routes/show', route: route %>
    <% end %>
  </div>
</div>
