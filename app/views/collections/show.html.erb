<div class="wrapper">

<div>
  <h1 class="section__title--collection"><%= @collection.title %></h1>
  <div id="h5" class="section__description"><%= @collection.description %></div>
</div>

  <div class="route-list__wrapper">
    <% @routes.each do |route| %>
      <%= render 'routes/show', route: route %>
    <% end %>
  </div>

</div>
