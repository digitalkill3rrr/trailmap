<h4>
  <%= comment.user.nickname %>
  <div>
    <small> <%= time_ago_in_words(comment.created_at) %> назад </small>
  </div>
</h4>
<p><%= comment.body %></p>

<% if can? :update, Comment %>
  <%= link_to 'Edit', edit_comment_path(comment) unless comment.new_record? %>
<% end %>

<hr>
